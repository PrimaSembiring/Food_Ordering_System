[app:main]
use = egg:Food_Ordering_System

pyramid.reload_templates = true
pyramid.debug_all = true
pyramid.includes =
    pyramid_tm
    pyramid_retry

[server:main]
use = egg:waitress
listen = localhost:8000
